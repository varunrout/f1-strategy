[project]
name = "f1-strategy"
version = "0.1.0"
description = "F1 Race Strategy Analysis: Tyre Degradation, Traffic Dynamics, and Expected Threat"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
